# Remote State Backend Configuration Template
# Copy this file to backend.tf and populate with your S3 bucket and DynamoDB table.

# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket-name"
#     key            = "recovery-engine/dev/terraform.tfstate"
#     region         = "ap-south-1"
#     dynamodb_table = "terraform-state-locks"
#     encrypt        = true
#   }
# }
